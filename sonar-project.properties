sonar.projectKey=seu-usuario_farma
sonar.organization=seu-usuario

# Project metadata
sonar.projectName=Guia Visual Interativo - Controle de Qualidade Farmacêutico
sonar.projectVersion=1.0.0

# Path to source directories
sonar.sources=src
sonar.tests=tests

# Encoding of the source files
sonar.sourceEncoding=UTF-8

# Language
sonar.language=js

# Coverage exclusions
sonar.coverage.exclusions=**/*.test.js,**/*.spec.js,**/tests/**/*

# Test coverage
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Duplicate code detection
sonar.cpd.js.minimumTokens=100

# Security hotspots
sonar.security.sources.javasecurity=src

# Quality gate
sonar.qualitygate.wait=true

# Analysis mode
sonar.analysis.mode=publish

# Exclude patterns
sonar.exclusions=**/node_modules/**,**/dist/**,**/build/**,**/*.min.js,**/*.min.css

# Include patterns
sonar.inclusions=**/*.js,**/*.jsx,**/*.ts,**/*.tsx,**/*.html,**/*.css